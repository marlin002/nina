<nav class="bg-slate-800 text-white fixed top-0 left-0 right-0 z-50 shadow-lg">
  <div class="container mx-auto px-3 sm:px-5 py-2 sm:py-3">
    <div class="flex items-center justify-between gap-2 sm:gap-4">
      <!-- Logo -->
      <div class="flex-shrink-0">
        <%= link_to "iAFS", root_path, class: "text-xl sm:text-2xl font-semibold hover:text-blue-300 transition" %>
      </div>
      
      <!-- Search Form -->
      <div class="flex-grow max-w-xs sm:max-w-md md:max-w-2xl mx-2 sm:mx-4">
        <%= form_with url: search_scrapes_path, method: :get, local: true, class: "flex gap-1 sm:gap-2" do %>
          <%= text_field_tag :q, params[:q], placeholder: t('search.form.placeholder'), autofocus: true, class: "flex-grow px-2 sm:px-4 py-2 text-sm sm:text-base rounded-lg border border-slate-600 bg-slate-700 text-white placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500" %>
          <button type="submit" class="px-3 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg transition cursor-pointer" aria-label="<%= t('buttons.search') %>">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
            </svg>
          </button>
        <% end %>
      </div>
      
      <!-- Dropdown Menu -->
      <div class="flex-shrink-0 relative" data-controller="dropdown">
        <button class="p-2 hover:bg-slate-700 rounded-lg transition" data-action="click->dropdown#toggle" type="button" aria-label="Meny">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
        <div class="absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-lg hidden transition-all duration-200" data-dropdown-target="menu">
          <%= link_to "Om", about_scrapes_path, class: "block px-4 py-3 text-slate-700 hover:bg-slate-100 transition first:rounded-t-lg" %>
          <%= link_to "Alla AFSar", all_scrapes_path, class: "block px-4 py-3 text-slate-700 hover:bg-slate-100 transition last:rounded-b-lg" %>
        </div>
      </div>
    </div>
  </div>
</nav>
